{
  "name": "api-signal-explorer-desktop",
  "version": "0.1.0",
  "description": "Electron-based API Signal Explorer — Octoparse-style with native browser and signal correlation",
  "main": "main.js",
  "scripts": {
    "build": "tsc -p ../src/main/tsconfig.json && tsc",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "test:stealth": "electron test-stealth-patches.js",
    "rebuild:mac": "node scripts/rebuild-mac-addon.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^32.0.1",
    "node-gyp": "^10.2.0",
    "typescript": "^5.3.0"
  }
}
