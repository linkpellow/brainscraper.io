{
  "name": "explorer-desktop",
  "version": "0.1.0",
  "description": "API Signal Explorer — Electron desktop with embedded proxied browser (mitmproxy) + Next.js Explorer UI",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^32.0.1",
    "typescript": "^5.3.0"
  }
}
